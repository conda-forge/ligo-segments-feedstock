--- /Users/duncan/src/ligo-segments-1.2.0/src/segmentlist.c	2018-11-24 09:56:11.000000000 +0000
+++ /Users/duncan/src/ligo-segments-1.2.0/src/segmentlist.c.1	2019-02-04 10:01:35.000000000 +0000
@@ -196,7 +196,7 @@
 }


-static PyObject *min(PyObject *a, PyObject *b)
+static PyObject *slmin(PyObject *a, PyObject *b)
 {
 	int result;

@@ -215,7 +215,7 @@
 }


-static PyObject *max(PyObject *a, PyObject *b)
+static PyObject *slmax(PyObject *a, PyObject *b)
 {
 	int result;

@@ -340,14 +340,14 @@
 			return NULL;
 		}

-		lo = min(lo, item_lo);
+		lo = slmin(lo, item_lo);
 		if(!lo) {
 			Py_DECREF(hi);
 			Py_DECREF(item_hi);
 			return NULL;
 		}

-		hi = max(hi, item_hi);
+		hi = slmax(hi, item_hi);
 		if(!hi) {
 			Py_DECREF(lo);
 			Py_DECREF(item_lo);
@@ -720,7 +720,7 @@
 				Py_DECREF(b);
 				return NULL;
 			} else if(result > 0) {
-				hi = max(hi, b);
+				hi = slmax(hi, b);
 				if(!hi) {
 					Py_DECREF(lo);
 					return NULL;
@@ -905,7 +905,7 @@
 				Py_DECREF(other);
 				return NULL;
 			}
-			hi = max(hi, item_hi);
+			hi = slmax(hi, item_hi);
 			if(!hi) {
 				Py_DECREF(lo);
 				Py_DECREF(other);
