{% set name = "ligo-segments" %}
{% set version = "1.1.0" %}
{% set sha256 = "6581abed50c13d273bb7d25a8eb104222090e3831885d1fea73bfba91518af1b" %}

package:
  name: {{ name }}
  version: {{ version }}

source:
  fn: {{ name }}-{{ version }}.tar.gz
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/{{ name }}-{{ version }}.tar.gz
  sha256: {{ sha256 }}
  patches:
    - pure-python-windows.patch  # [win]

build:
  number: 1000
  script: python -m pip install --no-deps --ignore-installed .

requirements:
  build:
    - {{ compiler('c') }}  # [not win]
  host:
    - python
    - setuptools
    - pip
  run:
    - python
    - six
    - ligo-common

test:
  imports:
    - ligo.segments

about:
  home: https://git.ligo.org/lscsoft/{{ name }}
  license: GPLv3
  license_family: GPL
  license_file: LICENSE
  summary: Representations of semi-open intervals
  description: |
    This module defines the segment and segmentlist objects, as well as the
    infinity object used to define semi-infinite and infinite segments.

extra:
  recipe-maintainers:
    - duncanmmacleod
