--- setup.py	2018-10-14 08:42:48.000000000 +0100
+++ setup.py	2019-01-29 10:58:08.000000000 +0000
@@ -62,13 +62,13 @@
 	cmdclass = cmdclass,
 	setup_requires = setup_requires,
 	install_requires = ['six', 'ligo-common'],
-	ext_modules = [
-		Extension(
-			'ligo.segments.__segments',
-			['src/segments.c', 'src/infinity.c', 'src/segment.c', 'src/segmentlist.c'],
-			include_dirs = ['src'],
-		),
-	],
+	#ext_modules = [
+	#	Extension(
+	#		'ligo.segments.__segments',
+	#		['src/segments.c', 'src/infinity.c', 'src/segment.c', 'src/segmentlist.c'],
+	#		include_dirs = ['src'],
+	#	),
+	#],
 	classifiers = [
 		'Development Status :: 5 - Production/Stable',
 		'Programming Language :: Python',
